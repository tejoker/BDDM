-- Auto-generated by repair_bad_translations.py
-- paper_id: 2604.21314
-- base paper theory import: enabled

import Mathlib
import Aesop
import Desol.PaperTheory.Paper_2604_21314

open MeasureTheory ProbabilityTheory Filter Set
open scoped BigOperators

namespace Paper_2604_21314_Repair

-- symbol: h_weak_solution_weak2_called_1 -> h_weak_solution_weak2_called_1 (paper_scalar_or_distribution)
-- symbol: h_weak_solution_weak_called_1 -> h_weak_solution_weak_called_1 (paper_scalar_or_distribution)
-- symbol: [Weak solution]\label{Weak} Let $v(0)\in L^1_{\text{loc}}(\mathbbm{H}^N, \|\xi\|^{-2\mu(\lambda)}d\xi),f\in L^1_{\text{loc}}(\mathbbm{H}^N, \|\xi\|^{-\mu(\lambda)}d\xi).$ Then $v\in L^p_{\text{loc}}((0,T),L^p_{\text{loc}}(\mathbbm{H}^N,\|\xi\|_H^{-{(p+1)\mu(\lambda)}}d\xi))\cap L^1_{\text{loc}}((0,T),L^1_{\text{loc}}(\mathbbm{H}^N,\|\xi\|_H^{-2\mu(\lambda)}d\xi)) $ is called a local-in-time weak solution to \eqref{eq equi} (and equivalently, to \eqref{eq 0.1}) if \begin{align} -\int_0^T\int_{\ma -> WeakSourceStatement (source_backed_review_statement_symbol)
-- symbol: [Weak solution]\label{Weak2} Let $u_0,f\in L^1_{\text{loc}}(\mathbbm{H}^N).$ Then $u\in L^p_{\text{loc}}((0,T),L^p_{\text{loc}}(\mathbbm{H}^N))$ is called a local-in-time weak solution to \eqref{eq without} if \begin{align} -\int_0^T\int_{\mathbbm{H}^N}u(\partial_t\phi+\Delta_{H}\phi)d\xi dt =\int_0^T\int_{\mathbbm{H}^N}|u|^pI_{T^-}^\gamma\phi d\xi dt +\int_{\mathbbm{H}^N}u_0\phi(0,\cdot)d\xi +\int_0^T\int_{\mathbbm{H}^N} \tau^\alpha f\phi d\xi d\tau, \end{align} holds for any non-negative test -> Weak2SourceStatement (source_backed_review_statement_symbol)
-- symbol: [Riemann-Liouville fractional integral \cite{Kilbas}] For a given integrable function $u(t),$ $t\in (0,T),$ the left and right Riemann–Liouville fractional integrals of order $\gamma\in (0,1)$ are defined as \begin{align} I_{0+}^\gamma u(t)=\frac{1}{\Gamma(\gamma)}\int_0^t(t-\tau)^{\gamma-1}u(\tau)d\tau, \end{align} and \begin{align} I_{T-}^\gamma u(t)=\frac{1}{\Gamma(\gamma)}\int_t^T(\tau-t)^{\gamma-1}u(\tau)d\tau, \end{align} respectively. Moreover, $I_{T-}^\gamma u, I_{0+}^\gamma u\longrighta -> Defn8SourceStatement (source_backed_review_statement_symbol)
-- symbol: % It is easy to observe using Theorem \ref{Local} that the following equation has a local-in-time solution: % \begin{align} % \begin{cases} % \partial_tu_\varepsilon-\Delta_{\mathbbm{H}^N}u_\varepsilon=\lambda \frac{\psi u_\varepsilon}{\|\cdot\|^{2}_{\mathbbm{H}^N}+\varepsilon}+\frac{1}{\Gamma(\gamma)}\int_0^t(t-\tau)^{\gamma-1}|u_\varepsilon(\tau)|^{p}d\tau+t^\alpha f &\text{in } \,\mathbbm{H}^N\times (0,T),\\ % u_\varepsilon(0,\cdot)=u_0(\cdot) &\text{in } \,\mathbbm{H}^N, % \end{cases} % \end -> Rem21SourceStatement (source_backed_review_statement_symbol)

-- Domain axioms / paper-local symbols. These are explicit formalization debt.
def h_weak_solution_weak2_called_1 : ℝ := 0

def h_weak_solution_weak_called_1 : ℝ := 0

def WeakSourceStatement : Prop := True

def Weak2SourceStatement : Prop := True

def Defn8SourceStatement : Prop := True

def Rem21SourceStatement : Prop := True

end Paper_2604_21314_Repair
