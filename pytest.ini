[pytest]
addopts = --basetemp=/mnt/NVME1/pytest_tmp
markers =
    slow: tests that hit lake/REPL/Mistral/HTTP and take > 2s. Skip with `-m 'not slow'`.
    integration: end-to-end tests that exercise multiple subsystems together.
